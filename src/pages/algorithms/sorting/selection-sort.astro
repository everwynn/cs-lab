---
import BaseLayout from '../../../layouts/BaseLayout.astro';
import SortingVisualizer from '../../../components/algorithms/SortingVisualizer.vue';
---
<BaseLayout title="选择排序动画演示 - Demo Center" description="选择排序算法动画演示，逐步骤可视化查找最小值和交换过程，Java 代码同步高亮">
  <article class="max-w-5xl mx-auto px-6 py-10">
    <a href="/algorithms" class="inline-flex items-center gap-2 text-sm text-gray-600 dark:text-slate-400 hover:text-accent-dark dark:hover:text-accent transition-colors mb-6">
      <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 19l-7-7m0 0l7-7m-7 7h18"/></svg>
      返回算法列表
    </a>
    <header class="mb-8">
      <div class="flex items-center gap-3 mb-3">
        <span class="text-3xl">🎯</span>
        <div>
          <div class="flex items-center gap-2 mb-1">
            <span class="px-2 py-0.5 text-xs font-medium rounded-full bg-purple-100 dark:bg-purple-900/30 text-purple-700 dark:text-purple-300 border border-purple-200 dark:border-purple-800">排序</span>
            <span class="px-2 py-0.5 text-xs font-medium rounded-full bg-orange-100 dark:bg-orange-900/30 text-orange-700 dark:text-orange-300 border border-orange-200 dark:border-orange-800">不稳定</span>
            <span class="px-2 py-0.5 text-xs font-medium rounded-full bg-red-100 dark:bg-red-900/30 text-red-700 dark:text-red-300 border border-red-200 dark:border-red-800">O(n²)</span>
          </div>
          <h1 class="text-2xl md:text-3xl font-bold text-gray-900 dark:text-white">选择排序 (Selection Sort)</h1>
        </div>
      </div>
      <p class="text-gray-600 dark:text-slate-400 max-w-3xl leading-relaxed">
        每轮从未排序部分中选出最小值，与当前位置交换。相比冒泡排序，交换次数大幅减少（每轮最多交换一次），但比较次数不变。
      </p>
      <div class="flex flex-wrap gap-2 mt-4">
        {['选择最小值', '单次交换', '原地排序', '不稳定'].map(s => (
          <span class="px-3 py-1 text-xs rounded-full bg-gray-100 dark:bg-slate-800 text-gray-600 dark:text-slate-400 border border-gray-200 dark:border-slate-700">{s}</span>
        ))}
      </div>
    </header>
    <SortingVisualizer client:load algorithm="selection" initialArray={[64, 25, 12, 22, 11, 36, 48, 19]} />
    <section class="mt-12 space-y-6">
      <h2 class="text-xl font-bold text-gray-900 dark:text-white">算法详解</h2>
      <div class="p-5 rounded-xl border border-gray-200 dark:border-slate-700 bg-white dark:bg-slate-900/40 space-y-3">
        <h3 class="text-sm font-bold text-gray-900 dark:text-white">核心思想</h3>
        <p class="text-sm text-gray-600 dark:text-slate-400 leading-relaxed">
          将数组分为已排序和未排序两部分。每轮在未排序部分中找到最小值，将其与未排序部分的第一个元素交换。经过 n-1 轮后，整个数组有序。
          紫色柱子表示当前记录的最小值索引（minIdx），随着比较进行可能不断更新。
        </p>
      </div>
      <div class="p-5 rounded-xl border border-gray-200 dark:border-slate-700 bg-white dark:bg-slate-900/40 space-y-3">
        <h3 class="text-sm font-bold text-gray-900 dark:text-white">复杂度分析</h3>
        <div class="overflow-x-auto">
          <table class="w-full text-sm text-gray-600 dark:text-slate-400">
            <thead>
              <tr class="border-b border-gray-200 dark:border-slate-700">
                <th class="text-left py-2 pr-4 font-medium">场景</th>
                <th class="text-left py-2 pr-4 font-medium">时间复杂度</th>
                <th class="text-left py-2 font-medium">说明</th>
              </tr>
            </thead>
            <tbody class="divide-y divide-gray-100 dark:divide-slate-800">
              <tr><td class="py-2 pr-4">最优</td><td class="py-2 pr-4 font-mono text-green-600 dark:text-green-400">O(n²)</td><td class="py-2 text-xs">无论是否有序，都需要完整比较</td></tr>
              <tr><td class="py-2 pr-4">平均</td><td class="py-2 pr-4 font-mono text-amber-600 dark:text-amber-400">O(n²)</td><td class="py-2 text-xs">约 n²/2 次比较，最多 n 次交换</td></tr>
              <tr><td class="py-2 pr-4">最差</td><td class="py-2 pr-4 font-mono text-red-500 dark:text-red-400">O(n²)</td><td class="py-2 text-xs">逆序时每次都需要交换</td></tr>
              <tr><td class="py-2 pr-4">空间</td><td class="py-2 pr-4 font-mono text-blue-600 dark:text-blue-400">O(1)</td><td class="py-2 text-xs">原地排序</td></tr>
            </tbody>
          </table>
        </div>
      </div>
    </section>
  </article>
</BaseLayout>
